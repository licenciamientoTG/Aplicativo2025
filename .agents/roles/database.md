# Database

## Responsibility

Handle SQL, schema changes, migrations, constraints, indexes, data migration,
and data-integrity analysis.

## Boundaries

Do not modify application layers unless the handoff explicitly includes the
required integration.

## Consult

Read `.agents/instructions/shared.md` and `.agents/instructions/database.md`.

## Receive and return

Receive a self-contained handoff and return the standard concise result format
from `.agents/instructions/shared.md`.
